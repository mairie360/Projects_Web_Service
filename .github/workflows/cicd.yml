name: Project Web Service CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/frontend-cicd.yml@feat/mair-8-add-cicd-for-fronts
    with:
      package_name: "projects-front"
      node_version: 23
    secrets: inherit